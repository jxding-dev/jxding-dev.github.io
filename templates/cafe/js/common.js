document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-modal-open]').forEach(btn=>btn.addEventListener('click',()=>{const modal=document.querySelector(btn.dataset.modalOpen);if(modal){modal.classList.add('is-open');modal.querySelector('button,[href],input,textarea,select')?.focus();}}));document.querySelectorAll('[data-modal-close],.modal-overlay').forEach(el=>el.addEventListener('click',e=>{if(e.target===el||el.hasAttribute('data-modal-close')) el.closest('.modal-overlay')?.classList.remove('is-open');}));document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelectorAll('.modal-overlay.is-open').forEach(m=>m.classList.remove('is-open'));});document.querySelectorAll('.faq-question').forEach(btn=>btn.addEventListener('click',()=>{const item=btn.closest('.faq-item');const open=btn.getAttribute('aria-expanded')==='true';btn.setAttribute('aria-expanded',String(!open));item?.classList.toggle('is-open',!open);}));});